#pragma once

namespace ECS
{
    using size                                  = size_t;
    
    const size ComponentCount                   = 8;
    const size MAX_ENTITIES                     = 100;
    const size INVALID_ENTITY                   = static_cast<size>(-1);
}

